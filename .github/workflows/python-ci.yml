name: Human Ergonomics Analysis - CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install dependencies
        working-directory: Human_Ergonomics_Analysis-main
        run: pip install -r requirements.txt

      - name: Check Python syntax
        working-directory: Human_Ergonomics_Analysis-main
        run: python -m compileall .

      - name: Verify Python
        run: |
          python --version
          pip --version
